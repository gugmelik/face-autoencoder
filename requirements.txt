torch>=2.1.0
torchvision>=0.16.0
# InsightFace_Pytorch is not pip-installable. Clone it and set INSIGHTFACE_PYTORCH_PATH:
#   git clone https://github.com/TreB1eN/InsightFace_Pytorch ../InsightFace_Pytorch
# Then download the IR-SE50 weights linked in that repo's README.
pytorch-msssim>=0.2.1
Pillow>=10.0.0
numpy>=1.24.0
tqdm>=4.65.0
PyYAML>=6.0
wandb>=0.16.0
huggingface_hub>=0.23.0
# Evaluation only: INDEPENDENT identity verifier (InsightFace buffalo_l /
# w600k_r50, ONNX) — deliberately a different network + dataset than the
# IR-SE50/MS1M training teacher. Use onnxruntime-gpu instead for CUDA.
insightface>=0.7.3
onnxruntime>=1.17.0
# Evaluation metrics & figures: LPIPS, FID, prior-diagnostic histogram.
lpips>=0.1.4
torchmetrics>=1.0.0
torch-fidelity>=0.3.0        # backend for torchmetrics FID
matplotlib>=3.7.0
